# `min-h-svh` use for height.... we use `flex-1` ... flex grow to cover what the width
...svh..smallest viewport height.... the smallest height that view port could be....

flex1 to grow and put the input in responsive layout
